<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pucmm.proyecto_final">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proyecto_final">
        <activity
            android:name=".room_view_model.activities.CategoryForm"
            android:exported="false" />
        <activity
            android:name=".room_view_model.activities.MainCategory"
            android:exported="false" />
        <activity
            android:name=".room_view_model.activities.MainProduct"
            android:exported="false" />
        <activity
            android:name=".ui.ProductForm"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.Proyecto_final.NoActionBar" />
        <activity android:name=".data.ForgotPassword" />
        <activity android:name=".data.Register" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>